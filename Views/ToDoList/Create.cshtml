@model ToDoListModel
@{
    ViewData["Title"] = "Add Another Itemr";
}
<div>
    <form asp-action="Create">
        <div class="form-group">
            <label>Title</label>
            <input asp-for="Title" class="form-control" />
            <br />
        </div>
        <div class="form-group">
            <label>Status</label>
            <input asp-for="isChecked" class="form-control" />
        </div>
        <div class="form-group">
            <button type="submit">Add</button>
        </div>
    </form>
</div>